.block
	.secondary-navigation
		%ul.wat-cf
			%li.first= link_to "Wszyscy użytkownicy", users_path
			%li.active= link_to "Edycja", edit_user_path
	.content
		%h2.title
			Edycja informacji profilowych
			= @user.username
		.inner
			= form_for @user, :url => user_path(@user), :html => { :class => :form } do |f|
				= render :partial => "form", :locals => {:f => f}
-# content_for :sidebar, render(:partial => 'sidebar')
