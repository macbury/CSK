.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "Wszystkie zgłoszenia", applications_path
      %li.active= link_to "Nowe zgłoszenie", new_application_path
  .content
    %h2.title
      Nowe zgłoszenie
    .inner
      = form_for :application, :url => applications_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
-# content_for :sidebar, render(:partial => 'sidebar')
